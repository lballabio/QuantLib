add_executable(FedFundsFuturesCurve FedFundsFuturesCurve.cpp)
target_link_libraries(FedFundsFuturesCurve ql_library ${QL_THREAD_LIBRARIES})
if (QL_INSTALL_EXAMPLES)
    install(TARGETS FedFundsFuturesCurve RUNTIME DESTINATION ${QL_INSTALL_EXAMPLESDIR})
endif()
