name: Coverage report
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:
jobs:
  coverage:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v7
    - name: Setup
      run: |
        sudo rm /etc/apt/sources.list.d/microsoft-prod.list
        sudo apt update
        sudo apt install -y lcov libboost-dev
    - name: Compile
      run: |
        ./autogen.sh
        ./configure --disable-shared CXXFLAGS='-O1 -fprofile-arcs -ftest-coverage' LDFLAGS='-lgcov'
        make -j 4
    - name: Capture baseline
      run: |
        mkdir -p coverage tmp
        lcov --no-external --capture --initial --directory . --output-file ./tmp/lcov_base.info --ignore-errors mismatch,empty,source
    - name: Run tests
      run: |
        ./test-suite/quantlib-test-suite --log_level=message
    - name: Run examples
      run: |
        make -C Examples check-examples
    - name: Capture coverage
      run: |
        lcov --no-external --capture --directory . --output-file ./tmp/lcov_run.info --ignore-errors mismatch,empty,source
        lcov --add-tracefile ./tmp/lcov_base.info --add-tracefile ./tmp/lcov_run.info --output-file ./tmp/lcov_total.info
        lcov --remove ./tmp/lcov_total.info "$PWD/Examples/*" "$PWD/test-suite/*" --output-file ./coverage/lcov.info
    - name: Upload coverage to Coveralls
      # Forks are often not registered on Coveralls; don't fail CI when upload
      # returns 422 "Couldn't find a repository matching this job."
      uses: coverallsapp/github-action@v2
      continue-on-error: true
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        file: ./coverage/lcov.info
        fail-on-error: false
